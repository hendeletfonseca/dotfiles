[defaults]
inventory = inventory.ini
connection = local
gathering = explicit
log_path = ./ansible.log

[privilege_escalation]
become_method = sudo
become_user = root
become_ask_pass = False
timeout = 10